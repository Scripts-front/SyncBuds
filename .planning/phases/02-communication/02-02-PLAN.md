---
phase: 02-communication
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - SyncBuds/SyncBuds.entitlements
  - SyncBuds/Info.plist
autonomous: true
requirements:
  - COM-01
must_haves:
  truths:
    - "macOS sandbox entitlements permit Multipeer Connectivity network access"
    - "iOS Info.plist declares local network usage and Bonjour service type for MCF"
  artifacts:
    - path: "SyncBuds/SyncBuds.entitlements"
      provides: "macOS sandbox network entitlements for Multipeer Connectivity"
      contains: "com.apple.security.network.client"
    - path: "SyncBuds/Info.plist"
      provides: "iOS local network permission + Bonjour service declaration"
      contains: "NSLocalNetworkUsageDescription"
  key_links:
    - from: "SyncBuds/SyncBuds.entitlements"
      to: "MCNearbyServiceAdvertiser / MCNearbyServiceBrowser"
      via: "macOS sandbox network.client + network.server entitlements"
      pattern: "com.apple.security.network.client"
    - from: "SyncBuds/Info.plist"
      to: "MCNearbyServiceAdvertiser (iOS)"
      via: "NSBonjourServices declaring _syncbuds-bt._tcp and _syncbuds-bt._udp"
      pattern: "_syncbuds-bt"
---

<objective>
Add the platform-level permissions required for Multipeer Connectivity to discover and connect peers. Without these, MCF silently fails — the advertiser starts but no peers are ever found.

Purpose: Blocking pitfall prevention. Pitfalls 2 and 3 from the research document both cause silent peer discovery failure. These must be in place before MultipeerService is instantiated.

Output: Updated `SyncBuds.entitlements` (macOS network sandbox entries) + updated `SyncBuds/Info.plist` (iOS local network permission + Bonjour services declaration).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-communication/02-RESEARCH.md
</context>

<interfaces>
<!-- Current file contents the executor must read before editing -->

SyncBuds/SyncBuds.entitlements currently contains:
- com.apple.security.app-sandbox = true
- com.apple.security.device.bluetooth = true
- CloudKit keys (commented out, to remain commented out — do NOT remove them)

SyncBuds/Info.plist currently contains:
- NSBluetoothAlwaysUsageDescription only
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add network entitlements to macOS sandbox</name>
  <files>SyncBuds/SyncBuds.entitlements</files>
  <read_first>
    - SyncBuds/SyncBuds.entitlements — read the full current content before editing
    - .planning/phases/02-communication/02-RESEARCH.md — Pitfall 3 (macOS sandbox network entitlements)
  </read_first>
  <action>
Read the current SyncBuds/SyncBuds.entitlements. Then add two new entitlement keys inside the existing `<dict>` block, after the `com.apple.security.device.bluetooth` entry and before the closing `</dict>` tag:

```xml
    <!-- Multipeer Connectivity — required for MCNearbyServiceAdvertiser/Browser (Pitfall 3) -->
    <key>com.apple.security.network.client</key>
    <true/>
    <key>com.apple.security.network.server</key>
    <true/>
```

Do NOT remove or modify the existing keys (app-sandbox, device.bluetooth) or the commented-out CloudKit block.

The resulting file must contain all of:
- com.apple.security.app-sandbox
- com.apple.security.device.bluetooth
- com.apple.security.network.client
- com.apple.security.network.server
- The CloudKit comment block (unchanged)
  </action>
  <verify>
    <automated>grep -c "com.apple.security.network" /root/SyncBuds/SyncBuds/SyncBuds.entitlements</automated>
  </verify>
  <acceptance_criteria>
    - grep "com.apple.security.network.client" SyncBuds/SyncBuds.entitlements exits 0
    - grep "com.apple.security.network.server" SyncBuds/SyncBuds.entitlements exits 0
    - grep "com.apple.security.app-sandbox" SyncBuds/SyncBuds.entitlements exits 0 (existing key preserved)
    - grep "com.apple.security.device.bluetooth" SyncBuds/SyncBuds.entitlements exits 0 (existing key preserved)
    - xcodebuild build -scheme SyncBuds -destination 'platform=macOS' succeeds (no plist parse errors)
  </acceptance_criteria>
  <done>macOS entitlements file contains both network.client and network.server keys alongside existing keys. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Add iOS local network + Bonjour Info.plist entries</name>
  <files>SyncBuds/Info.plist</files>
  <read_first>
    - SyncBuds/Info.plist — read the full current content before editing
    - .planning/phases/02-communication/02-RESEARCH.md — Pitfall 2 (NSLocalNetworkUsageDescription + NSBonjourServices)
  </read_first>
  <action>
Read the current SyncBuds/Info.plist. Then add two new keys inside the existing `<dict>` block, after the `NSBluetoothAlwaysUsageDescription` entry and before the closing `</dict>` tag:

```xml
    <key>NSLocalNetworkUsageDescription</key>
    <string>SyncBuds uses your local network to communicate between your Mac and iPhone for headphone switching.</string>
    <key>NSBonjourServices</key>
    <array>
        <string>_syncbuds-bt._tcp</string>
        <string>_syncbuds-bt._udp</string>
    </array>
```

Note: The service type `syncbuds-bt` (11 characters, lowercase letters and hyphen only) matches the service type constant used in MultipeerService (Plan 03). Do NOT use a different string here.

The resulting file must contain all of:
- NSBluetoothAlwaysUsageDescription (existing, unchanged)
- NSLocalNetworkUsageDescription
- NSBonjourServices array with both _syncbuds-bt._tcp and _syncbuds-bt._udp
  </action>
  <verify>
    <automated>grep -c "syncbuds-bt" /root/SyncBuds/SyncBuds/Info.plist</automated>
  </verify>
  <acceptance_criteria>
    - grep "NSLocalNetworkUsageDescription" SyncBuds/Info.plist exits 0
    - grep "_syncbuds-bt._tcp" SyncBuds/Info.plist exits 0
    - grep "_syncbuds-bt._udp" SyncBuds/Info.plist exits 0
    - grep "NSBluetoothAlwaysUsageDescription" SyncBuds/Info.plist exits 0 (existing key preserved)
    - xcodebuild build -scheme SyncBuds -destination 'platform=macOS' succeeds (plist is valid XML)
  </acceptance_criteria>
  <done>Info.plist contains NSLocalNetworkUsageDescription and NSBonjourServices with both TCP and UDP variants. Existing Bluetooth key preserved. Build succeeds.</done>
</task>

</tasks>

<verification>
1. `grep "com.apple.security.network.client" SyncBuds/SyncBuds.entitlements` — exits 0
2. `grep "com.apple.security.network.server" SyncBuds/SyncBuds.entitlements` — exits 0
3. `grep "NSLocalNetworkUsageDescription" SyncBuds/Info.plist` — exits 0
4. `grep -c "syncbuds-bt" SyncBuds/Info.plist` — returns 2 (both TCP and UDP)
5. `xcodebuild build -scheme SyncBuds -destination 'platform=macOS' -quiet` — exits 0
</verification>

<success_criteria>
- macOS entitlements: network.client + network.server added, all existing keys preserved
- iOS Info.plist: NSLocalNetworkUsageDescription + NSBonjourServices array with both service variants
- Both changes use exactly "syncbuds-bt" as the service type (no other string)
- Project compiles after edits (XML not corrupted)
</success_criteria>

<output>
After completion, create `.planning/phases/02-communication/02-02-SUMMARY.md` following the summary template.
</output>

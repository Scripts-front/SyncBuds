---
phase: 02-communication
plan: 05
type: execute
wave: 4
depends_on:
  - 02-04
files_modified: []
autonomous: false
requirements:
  - COM-01
  - BT-04
must_haves:
  truths:
    - "Mac and iPhone discover each other via Multipeer within 30 seconds on the same Wi-Fi"
    - "A status signal sent from Mac appears on iPhone UI within 500ms"
    - "A status signal sent from iPhone appears on Mac UI within 500ms"
    - "ContentView on both devices shows live peer status updating reactively"
    - "When one app is backgrounded, the other shows Peer offline"
  artifacts:
    - path: "SyncBuds/Shared/MultipeerService.swift"
      provides: "Verified end-to-end peer discovery and signal delivery"
  key_links:
    - from: "Mac ContentView"
      to: "iPhone ContentView"
      via: "MultipeerService signal delivery over local Wi-Fi"
      pattern: "manual verification"
---

<objective>
Verify the complete Multipeer Connectivity signaling pipeline on two real devices. This checkpoint cannot be automated — MCF peer discovery requires physical Mac + iPhone on the same Wi-Fi network.

Purpose: COM-01 success criterion 1 ("signal sent from Mac arrives on iPhone within 500ms") and BT-04 ("both apps display live connection status") can only be confirmed on real hardware.

Output: Human confirms or reports issues. No files modified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-communication/02-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Real-device verification — Multipeer peer discovery and status display</name>
  <files>none — verification only, no files modified</files>
  <action>
    Human action required. Run both apps on physical devices and verify signaling pipeline end-to-end.
    See how-to-verify below for exact test sequence.
  </action>
  <what-built>
    A complete Multipeer Connectivity signaling pipeline:
    - SyncSignal Codable struct (wire format for all messages)
    - MultipeerService (peer discovery, MCSession, status timer every 5s)
    - BluetoothManager wired to send .status signals on Bluetooth connect/disconnect
    - AudioRouteMonitor wired to send .status signals on audio route changes
    - ContentView updated on both platforms to display peer connection status
    - macOS sandbox entitlements: network.client + network.server
    - iOS Info.plist: NSLocalNetworkUsageDescription + NSBonjourServices for _syncbuds-bt
  </what-built>
  <read_first>
    - .planning/phases/02-communication/02-RESEARCH.md — Pitfall 5 (Multipeer foreground-only) for expected background behavior
  </read_first>
  <how-to-verify>
    **Setup:**
    1. Make sure your Mac and iPhone are on the same Wi-Fi network
    2. Build and run the SyncBuds app on your Mac (via Xcode, scheme: SyncBuds, destination: My Mac)
    3. Build and run the SyncBuds app on your iPhone (via Xcode, scheme: SyncBuds, destination: your iPhone)
    4. On iPhone: when prompted for local network permission, tap Allow

    **Test 1 — Peer Discovery:**
    5. Watch ContentView on both devices. Within 30 seconds, the "Peer Status" section on both devices should show a green dot and "Connected to [device name]" instead of the grey "Peer offline" message
    6. Expected: both devices show the connected state within ~10 seconds

    **Test 2 — Status signal delivery:**
    7. On Mac: click "Start Monitoring" then connect or disconnect your Bluetooth headphone
    8. Watch the iPhone's ContentView — "Headphone is on Mac" or "Headphone is available" should update within ~1 second
    9. On iPhone: connect or disconnect your Bluetooth headphone (or use headphone controls)
    10. Watch the Mac's ContentView — status should update within the 5-second timer interval

    **Test 3 — Status timer (BT-04):**
    11. Leave both apps in foreground for 10 seconds without touching either device
    12. Verify that even without a Bluetooth event, the status label stays current (5-second periodic refresh means it should not show stale data)

    **Test 4 — Background behavior (expected limitation, per D-07):**
    13. Send the iPhone app to the background (press Home or swipe up)
    14. Within ~30 seconds, the Mac's ContentView should show "Peer offline" (grey dot)
    15. Bring the iPhone app back to foreground — within ~30 seconds, both sides should show connected again
    16. This foreground-only behavior is EXPECTED and ACCEPTABLE — note it for the summary

    **Xcode console checks:**
    - Mac console should show: `[MultipeerService] Connected to: [iPhone name]`
    - iPhone console should show: `[MultipeerService] Connected to: [Mac name]`
    - Both consoles should show periodic received signal lines every 5 seconds when connected
  </how-to-verify>
  <verify>
    <automated>MISSING — real-device MCF verification cannot be automated. Human must run both apps simultaneously on Mac + iPhone.</automated>
  </verify>
  <acceptance_criteria>
    - Both devices show green "Connected to [peer name]" within 30 seconds of both apps launching
    - Bluetooth connect/disconnect on Mac causes iPhone status to update within 2 seconds
    - Audio route change on iPhone causes Mac status to update within 6 seconds (within one timer cycle)
    - Background behavior: Mac shows "Peer offline" when iPhone app is backgrounded
    - No crash on either device during the test sequence
  </acceptance_criteria>
  <done>Human types "verified" confirming all 4 tests passed. Phase 2 COM-01 and BT-04 requirements confirmed on real hardware.</done>
  <resume-signal>
    Type "verified" if all 4 tests passed, or describe any failures (e.g., "peers not discovering", "no status update on route change"). On failure, Claude will diagnose and create gap-closure plans.
  </resume-signal>
</task>

</tasks>

<verification>
Human confirms via resume-signal. No automated verification possible for real-device MCF behavior.
</verification>

<success_criteria>
- Peer discovery works within 30 seconds on same Wi-Fi
- Status signals flow in both directions within one timer cycle (5 seconds)
- ContentView displays reactive peer status on both platforms
- Background disconnection shows "Peer offline" (expected, acceptable)
- No crashes
</success_criteria>

<output>
After verification, create `.planning/phases/02-communication/02-05-SUMMARY.md`.

Include in the summary:
- Which tests passed/failed
- Observed discovery latency (seconds to first peer connection)
- Observed signal delivery latency
- Any unexpected behavior (unexpected disconnects, console errors)
- Background behavior observation
- Whether COM-01 and BT-04 success criteria are met
</output>
